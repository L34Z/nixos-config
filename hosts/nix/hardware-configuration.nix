# PLACEHOLDER — replaced during install by:
#   nixos-generate-config --no-filesystems --root /mnt
# (install.sh does this and copies the result here.)
# Filesystems/LUKS are intentionally absent: disko.nix owns those.
{ ... }:

{
}
